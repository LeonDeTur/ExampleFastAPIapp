services:
  example_api:
    build:
      context: .
      dockerfile: Dockerfile
    image: example-fastapi-app:dev
    container_name: example_fastapi_api
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      APP_VERSION: "0.0.1"
      APP_NAME: "Example FastAPI App"
      APP_DESCRIPTION: "Example FastAPI application running in Docker"
      DATABASE_URL: "postgresql://postgres:postgres@postgis_db:5432/gis_db"

    networks:
      -  example_network

    depends_on:
      postgis:
        condition: service_started

  postgis:
    image: postgis/postgis:16-3.4
    container_name: postgis_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: gis_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5434:5432"
    volumes:
      - postgis_data:/var/lib/postgresql/data
    networks:
      - example_network

networks:
  example_network:
    driver: bridge

volumes:
  postgis_data: